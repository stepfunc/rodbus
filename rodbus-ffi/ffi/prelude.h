#ifndef RODBUS_FFI_PRELUDE_H
#define RODBUS_FFI_PRELUDE_H

// Opaque pointer type for the Tokio runtime
typedef struct Runtime Runtime;

// Opaque pointer type for the Channel runtime
typedef struct Channel Channel;

#endif